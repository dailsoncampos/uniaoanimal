= form_with(model: donor, local: true) do |form|
  - if donor.errors.any?
    #error_explanation
      %h2
        = pluralize(donor.errors.count, "error")
        prohibited this donor from being saved:
      %ul
        - donor.errors.full_messages.each do |message|
          %li= message
  .form-control
    = form.text_field :name, class:'form-control mt-4', placeholder:'Nome Completo'
  
    = form.text_field :phone, class:'form-control mt-4', placeholder:'Telefone'
  
    = form.text_field :email, class:'form-control mt-4', placeholder:'Email'

    = form.select(:donation, @donations_options_for_select, class: "form-control")
  
    = form.text_area :message, class:'form-control mt-4', placeholder:'Descrição'
    .actions
      = form.submit "Confirmar", class:'col-lg-12 mt-4 btn btn-lg btn-login'